import type { Metadata } from "next";import Link from "next/link";import { PublicPage,CopySection } from "@/components/public-page";export const metadata:Metadata={title:"Workspace Unavailable",robots:{index:false,follow:false}};export default function Page(){return <PublicPage eyebrow="Service configuration" title="The workspace is temporarily unavailable." intro="Authentication is not configured for this deployment, so Seraphim has blocked access instead of silently starting local demo mode."><CopySection title="What to do"><p>The operator must configure the Supabase public URL and anonymous key, apply the required database migrations, and redeploy.</p><Link href="/support" className="font-bold text-brand-700">Contact support</Link></CopySection></PublicPage>}
