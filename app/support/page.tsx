import type { Metadata } from "next";
import Link from "next/link";
import { CopySection, PublicPage } from "@/components/public-page";
export const metadata: Metadata = { title: "Support" };
export default function SupportPage(){const email=process.env.NEXT_PUBLIC_SUPPORT_EMAIL;return <PublicPage eyebrow="Support" title="Get help with Seraphim." intro="Use the in-app Help page for workflow guidance. For account, privacy, or beta-access support, use the configured support channel below."><CopySection title="Before contacting support"><p>Include the safe error code shown in the application, the action you were taking, and the approximate time. Do not send API keys, passwords, full prospect lists, or raw confidential content.</p></CopySection><CopySection title="Support channel">{email?<p>Email <a className="font-bold text-brand-700" href={`mailto:${email}`}>{email}</a>. Do not include secrets or confidential prospect data.</p>:<p>The production support address has not yet been configured. The operator must set <code className="rounded bg-[#f1f1f5] px-1.5 py-1">NEXT_PUBLIC_SUPPORT_EMAIL</code> before public launch.</p>}<Link href="/login" className="font-bold text-brand-700">Sign in to your workspace</Link></CopySection></PublicPage>}
