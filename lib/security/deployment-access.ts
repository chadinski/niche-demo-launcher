import type { ServerUserAccess } from "@/lib/auth/server-guard";
function emailSet(value:string|undefined){return new Set((value||"").split(",").map(item=>item.trim().toLowerCase()).filter(Boolean))}
export function deploymentAccess(user:ServerUserAccess){if(process.env.AUTOMATED_DEPLOYMENT_ENABLED!=="1")return{allowed:false,reason:"Managed deployment is not enabled. Download the HTML instead."};if(user.mode!=="remote"||!user.email)return{allowed:false,reason:"Managed deployment requires an approved account."};const approved=new Set([...emailSet(process.env.DEPLOYMENT_APPROVED_EMAILS),...emailSet(process.env.ADMIN_EMAILS)]);return approved.has(user.email.toLowerCase())?{allowed:true,reason:"approved"}:{allowed:false,reason:"Managed deployment is limited to approved beta accounts. Download the HTML instead."}}
