import type { ServerUserAccess } from "@/lib/auth/server-guard";
import { createClient } from "@/lib/supabase/server";
export async function startGenerationJob(user:ServerUserAccess,generationId:string,requestId:string){if(user.mode!=="remote")return;const supabase=await createClient();if(!supabase)return;await supabase.from("generation_jobs").upsert({user_id:user.userId,generation_id:generationId,status:"running",stage:"starting",progress:2,request_id:requestId,started_at:new Date().toISOString(),completed_at:null,error_code:"",error_message:""},{onConflict:"user_id,generation_id"})}
export async function finishGenerationJob(user:ServerUserAccess,generationId:string,status:"succeeded"|"failed"|"cancelled",errorCode=""){if(user.mode!=="remote")return;const supabase=await createClient();if(!supabase)return;await supabase.from("generation_jobs").update({status,stage:status,progress:status==="succeeded"?100:0,error_code:errorCode,error_message:errorCode?"Generation stopped. Use the safe error code for support.":"",completed_at:new Date().toISOString()}).eq("user_id",user.userId).eq("generation_id",generationId)}
