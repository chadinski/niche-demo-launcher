export function friendlyAuthError(message:string){const value=message.toLowerCase();if(value.includes("invalid login"))return"Email or password is incorrect.";if(value.includes("email not confirmed"))return"Verify your email before signing in.";if(value.includes("already registered")||value.includes("user already"))return"An account may already exist for this email. Try signing in or resetting your password.";if(value.includes("rate")||value.includes("too many"))return"Too many attempts. Wait a moment and try again.";if(value.includes("password"))return"Use a stronger password with at least 10 characters.";return"Authentication could not be completed. Try again or contact support."}
