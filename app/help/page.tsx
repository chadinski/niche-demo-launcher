import type { Metadata } from "next";
import Link from "next/link";
import { Card } from "@/components/ui";
export const metadata: Metadata = { title: "Help", robots: { index:false, follow:false } };
export default function HelpPage(){return <div className="space-y-6"><div><p className="text-xs font-bold tracking-[.14em] text-brand-700 uppercase">Help center</p><h1 className="mt-2 text-4xl font-black tracking-[-.055em]">Work safely from lead to outreach.</h1><p className="mt-3 max-w-3xl leading-7 text-[#6f768a]">Verify facts before generation, review generated code and copy, export when ready, and keep every send manual.</p></div><div className="grid gap-4 lg:grid-cols-2">{[["Find or add a lead","Use Leads for discovery or Create Demo when you already have source material.","/leads"],["Create a first demo","Follow the business information, design, generation, review, and export stages.","/create"],["Understand usage","See plan allowances, consumed events, and failed or refunded operations.","/usage"],["Account and privacy","Export data, review legal policies, or permanently delete the account.","/account"]].map(([title,copy,href])=><Card key={title} className="p-6"><h2 className="text-xl font-extrabold">{title}</h2><p className="mt-2 leading-7 text-[#71788b]">{copy}</p><Link href={href} className="mt-4 inline-flex font-bold text-brand-700">Open guide →</Link></Card>)}</div></div>}
