import type { Metadata } from "next";
import { CopySection, PublicPage } from "@/components/public-page";
export const metadata: Metadata = { title: "How It Works", description: "See the guided Seraphim workflow from prospect research to reviewed outreach." };
const steps=["Add or find a business","Verify extracted information","Choose a design direction","Generate a fast draft or premium final","Review, refine, and export","Prepare outreach and track the prospect"];
export default function HowPage(){return <PublicPage eyebrow="How it works" title="A clear next step at every stage." intro="The workflow is designed around deliberate review. Automation does the heavy lifting; you stay responsible for business facts, website quality, deployment, and outreach."><div className="space-y-4">{steps.map((step,index)=><CopySection key={step} title={`${index+1}. ${step}`}><p>{["Import source material or search for a relevant local business.","Inspect evidence, confidence, and missing fields. Correct anything uncertain.","Use sensible defaults first; optional advanced controls remain available.","Usage is checked by the server before an external provider is called.","Preview safely, make changes, and download HTML. Approved accounts may use managed deployment.","Review the recipient and message, then send manually and update status."][index]}</p></CopySection>)}</div></PublicPage>}
