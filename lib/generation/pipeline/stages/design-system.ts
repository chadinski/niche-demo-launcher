export const DESIGN_SYSTEM_STAGE = {
  id: "design-system",
  event: "design-system",
  description: "Creates embedded CSS tokens and reusable classes so sections do not rely on dead utilities.",
} as const;
